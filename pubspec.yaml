name: mobile_tcc
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_functions: 0.0.1-dev  # ✅ CORRIGIDO - versão estável
  
  # UI & Design
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0
  carousel_slider: ^5.1.1
  fl_chart: ^1.1.1
  table_calendar: ^3.0.9
  flutter_spinkit: ^5.2.0
  
  # State Management & Forms
  provider: ^6.1.1
  get: ^4.6.6
  flutter_form_builder: ^10.2.0
  
  # Storage & Files
  shared_preferences: ^2.2.2
  path_provider: ^2.0.15
  open_filex: ^4.7.0
  image_picker: ^1.0.5
  permission_handler: ^12.0.1
  
  # HTTP & Networking
  http: ^1.5.0
  flutter_dotenv: ^6.0.0
  
  # Utilities
  package_info_plus: ^9.0.0
  intl: ^0.20.0
  
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0
  
  # Testing
  mockito: ^5.4.0
  build_runner: ^2.4.0
  table_calendar: ^3.0.9
  
flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/fundo-tcc-mobile.png
    # ✅ CONSIDERE ADICIONAR MAIS ASSETS SE NECESSÁRIO:
    # - lib/assets/images/
    # - lib/assets/icons/